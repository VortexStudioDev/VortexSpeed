/*
  This script sets isBlocked to false if it runs.
  If it's blocked (Adblockers should always block the script because the name is ads/ad.js),
  then isBlocked stays true.
*/
window.isBlocked = false;